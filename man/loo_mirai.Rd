% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel.R
\name{loo_mirai}
\alias{loo_mirai}
\title{Run a \pkg{loo} function over argument lists in parallel with mirai}
\usage{
loo_mirai(fun, args_list, n_daemons = NULL)
}
\arguments{
\item{fun}{A \pkg{loo} function (e.g. \code{\link[=loo]{loo()}}, \code{\link[=psis]{psis()}}) to apply to each
element of \code{args_list}.}

\item{args_list}{A list of lists. Each element is passed to \code{fun} via
\code{do.call(fun, element)}.}

\item{n_daemons}{Number of local daemons to start for this call. When
\code{NULL} (the default), an existing \code{\link[mirai:daemons]{mirai::daemons()}} pool is reused if
connected; otherwise the work runs serially. When an integer \verb{>= 2}, a
local pool of that size is created for the duration of the call and
torn down automatically when it returns (unless a pool was already
connected, in which case it is reused and left untouched).}
}
\value{
A list of results in the same order as \code{args_list}.
}
\description{
Intended for map-style workflows such as computing \code{\link[=loo]{loo()}}, \code{\link[=psis]{psis()}}, or
other \pkg{loo} functions for many models in one session.
}
\details{
Parallelism is controlled by \code{loo_mirai()} (via \code{n_daemons} or an
existing daemon pool), not by a \code{cores} argument on the mapped calls.
Any \code{cores} element in \code{args_list} is deprecated, ignored, and replaced
with \code{cores = 1} so each job runs serially on its worker.
}
\examples{
r_eff <- relative_eff(exp(example_loglik_array()))
ll_list <- list(example_loglik_matrix(), example_loglik_matrix())
args_list <- lapply(ll_list, function(LL) {
  list(log_ratios = -LL, r_eff = r_eff)
})

# Serial when no pool is connected and n_daemons is NULL
loo_mirai(psis, args_list)

\dontrun{
# Convenience wrapper: start workers, map, tear down
loo_mirai(psis, args_list, n_daemons = 2)

# Or manage the pool yourself and call loo functions directly
mirai::daemons(4)
loo_mirai(loo, lapply(ll_list, function(LL) {
  list(x = LL, r_eff = r_eff)
}))
mirai::daemons(0)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measures.R
\name{crps2}
\alias{crps2}
\title{Continuous ranked probability score (\code{crps})}
\usage{
crps2(
  y,
  ypred,
  log_weights = NULL,
  pointwise = NULL,
  scale = FALSE,
  revert_sign = FALSE
)
}
\arguments{
\item{y}{A numeric vector of observed outcomes.}

\item{ypred}{A numeric matrix of posterior predictive draws with dimensions
draws x observations.}

\item{log_weights}{Optional numeric matrix of unnormalized log-weights
with dimensions draws x observations.}

\item{pointwise}{Optional numeric vector of precomputed pointwise scores.
If provided, \code{y}, \code{ypred}, and \code{log_weights} are ignored.}

\item{scale}{Logical; if \code{TRUE}, computes the scaled CRPS.}

\item{revert_sign}{Logical; if \code{TRUE}, multiply the estimate and pointwise
values by -1 before returning.}
}
\value{
A named list with elements \code{estimate} (mean CRPS score), \code{se}
(standard error), and \code{pointwise} (pointwise CRPS scores).
}
\description{
Computes CRPS for continuous outcomes from posterior predictive draws using
weighted empirical distribution formulas. With \code{scale = TRUE}, returns the
scaled variant used by \code{\link[=scrps]{scrps()}}.
}
\examples{
y <- c(0.2, -0.1, 1.4)
ypred <- matrix(c(0.1, -0.2, 1.3, 0.3, 0.0, 1.7), nrow = 2)
crps2(y, ypred)
}

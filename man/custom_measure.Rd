% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pred_measure.R
\name{custom_measure}
\alias{custom_measure}
\title{Define a custom predictive measure}
\usage{
custom_measure(fun, name, se_diff_fun = NULL, loss = FALSE)
}
\arguments{
\item{fun}{A function that returns \code{estimate}, \code{se}, and \code{pointwise}. Only
the arguments \code{y}, \code{ypred}, \code{mupred}, \code{ylp}, and \code{log_weights} that are
in its signature are supplied.}

\item{name}{A character string. The measure is reported under this name.}

\item{se_diff_fun}{How \code{\link[=model_compare]{model_compare()}} computes the standard error of a
difference: a function \verb{(ref, cmp)}, \code{"sum"}, \code{"mean"}, or \code{NULL}. With
\code{NULL} (the default), the standard error of the difference is \code{NA}.}

\item{loss}{\code{TRUE} if lower values are better. The default \code{FALSE} treats
the measure as a utility.}
}
\value{
\code{fun} with the attributes \code{measure_name}, \code{measure_loss}, and
\code{measure_se_diff}.
}
\description{
Attaches the name, the orientation, and the standard error of the
difference to a measure function. Pass the result to the \code{measure}
argument of the \verb{*_pred_measure()} functions.
}
\examples{
my_abs_err <- custom_measure(
  function(y, mupred) {
    pw <- abs(y - colMeans(mupred))
    list(estimate = mean(pw), se = sd(pw) / sqrt(length(pw)), pointwise = pw)
  },
  name = "my_abs_err", se_diff_fun = "mean", loss = TRUE
)

}
\seealso{
\code{\link[=pred_measure]{pred_measure()}}, \code{\link[=model_compare]{model_compare()}}
}
